'use strict';

var assert = require('assert');
var convert = require('../lib');

/* Other Tests */
require('./error');

describe('convert-xls', function () {
  it('should output as CSV.', function () {
    assert(false, 'Not yet implemented.');
  });
  it('should output as JSON.', function () {
    assert(false, 'Not yet implemented.');
  });
  it('should output as accept files as multi-part form.', function () {
    assert(false, 'Not yet implemented.');
  });
  it('should accept buffers/streams.', function () {
    assert(false, 'Not yet implemented.');
  });
  it('should remove header/meta data from top of document.', function () {
    assert(false, 'Not yet implemented.');
  });
  it('should identify if meta/header data is in top of document', function () {
    assert(false, 'Not yet implemented.');
  });
});
